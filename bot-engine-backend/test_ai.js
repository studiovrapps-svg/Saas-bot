require('dotenv').config(); const { sendWhatsAppAI } = require('./src/services/ai.service'); sendWhatsAppAI('123', 'fake_token', '+50255555555', 'Hola', 3).then(() => console.log('Done')).catch(e => console.error('FAILED IN TEST:', e));
